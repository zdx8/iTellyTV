<?xml version="1.0" encoding="utf-8"?>
<!--
    Single channel row in the home list. We define it ourselves so
    we can add a clear "focused" state (the D-pad cursor that TVs
    rely on). The state list for the background is at
    res/drawable/channel_row_background.xml.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingHorizontal="24dp"
    android:paddingVertical="18dp"
    android:background="@drawable/channel_row_background"
    android:focusable="true"
    android:clickable="true">

    <TextView
        android:id="@+id/channel_favorite"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="★"
        android:textColor="#FFD54F"
        android:textSize="22sp"
        android:layout_marginEnd="12dp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/channel_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textColor="#FFFFFF"
        android:textSize="26sp" />

    <TextView
        android:id="@+id/channel_group"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#9CA3AF"
        android:textSize="18sp"
        android:layout_marginStart="16dp" />

</LinearLayout>
